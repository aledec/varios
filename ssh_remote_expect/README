ssh_expect.sh
 Script de copia de archivos remota/local y ejecucion de scripts remotos con path, sistema operativo y usuario de acuerdo a lo necesitado. Se usa expect
        # Parametros"
              [option]:"
                -s < SSH"
                -S < SCP"
                -Sr < SCP Reverso"
              [user]:"
                unixadm"
                root"
              [operating_system]:"
                linux"
                solaris"
                hpux"
                all"
              [location]:"
                artigas"
                cotia"
                all"
              [script]"


ssh.sh
 Script de copia de archivos remota/local y ejecucion de scripts remotos con path, sistema operativo y usuario de acuerdo a lo necesitado. 
        # Parametros"
              [option]:"
                -s < SSH"
                -S < SCP"
                -Sr < SCP Reverso"
              [user]:"
                unixadm"
                root"
              [operating_system]:"
                linux"
                solaris"
                hpux"
                all"
              [location]:"
                artigas"
                cotia"
                all"
              [script]"
